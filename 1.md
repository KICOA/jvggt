# Jittor 推理修复记录（本轮）

本轮在 WSL 上跑 `demo_jvggt.py --inference_only` 时，依次修复了 DPT 位置编码、ReLU、sign/expm1、后处理 device 等 Jittor/PyTorch API 差异。修改仅涉及 `jvggt/` 与 `tools/generate_jittor_port.py`，未动 `vggt/`、`training/`。

---

## 1. DPT 位置编码 H/W 颠倒

**现象**

```
Shape not match, x: float32[8,256,25,37]  y: float32[8,256,37,25]
```

发生在 `jvggt/heads/dpt_head.py` → `_apply_pos_embed` → `x + pos_embed`。

**原因**

PyTorch 使用 `torch.meshgrid(..., indexing="xy")`，输出形状为 `(height, width)`。Jittor 的 `jt.meshgrid` 默认等价于 `ij` 索引，输出为 `(width, height)`，导致 pos_embed 与 feature map 宽高对调。

**修改**

文件：`jvggt/heads/utils.py` → `create_uv_grid`

```python
uu, vv = jt.meshgrid(x_coords, y_coords)
uu, vv = uu.transpose(), vv.transpose()  # 对齐 PyTorch indexing="xy"
uv_grid = jt.stack((uu, vv), dim=-1)
```

**同步**：`tools/generate_jittor_port.py` 中 `heads/utils.py` 的转换规则已更新。

---

## 2. nn.ReLU(inplace=True) 不兼容

**现象**

```
TypeError: relu() got an unexpected keyword argument 'inplace'
```

发生在 `ResidualConvUnit.execute` → `self.activation(x)`，activation 来自 `nn.ReLU(inplace=True)`。

**原因**

Jittor 的 `nn.relu(x)` 不接受 `inplace` 参数，也不能像 PyTorch 那样把 `nn.ReLU(inplace=True)` 当可调用模块直接用。

**修改**

| 文件 | 改动 |
|------|------|
| `jvggt/ops.py` | 新增 `ReLU` Module，内部调用 `nn.relu(x)`，忽略 `inplace` |
| `jvggt/heads/dpt_head.py` | `from jvggt.ops import F, ReLU`；两处 `nn.ReLU(inplace=True)` 改为 `ReLU(inplace=True)` |

**同步**：`generate_jittor_port.py` 对 `heads/dpt_head.py` 增加 `nn.ReLU` → `ReLU` 替换。

---

## 3. sign / expm1 缺失

**现象**

```
AttributeError: 'jittor_core.Var' object has no attribute 'sign'
AttributeError: 'jittor_core.Var' object has no attribute 'expm1'
```

发生在 `point_head` 的 `activate_head` → `inverse_log_transform`（以及 `norm_exp` 分支中的 `d.expm1()`）。

**原因**

Jittor Var 没有 `.sign()`、`.expm1()`；`jt.sign` 也不存在。

**修改**

文件：`jvggt/ops.py`

```python
def sign(x):
    return jt.where(x > 0, 1.0, jt.where(x < 0, -1.0, 0.0))

def expm1(x):
    return x.exp() - 1.0
```

文件：`jvggt/heads/head_act.py`

```python
from jvggt.ops import F, expm1, sign

# norm_exp 分支
pts3d = (xyz / d) * expm1(d)

# inverse_log_transform
return sign(y) * expm1(y.abs())
```

**同步**：`generate_jittor_port.py` 对 `heads/head_act.py` 增加对应替换。

---

## 4. pose_enc 后处理 .device

**现象**

前向推理已跑通，在 `predictions_to_numpy` → `pose_encoding_to_extri_intri` 报错：

```
AttributeError: 'jittor_core.Var' object has no attribute 'device'
```

**原因**

Jittor Var 没有 `.device` 属性。

**修改**

文件：`jvggt/utils/pose_enc.py`

```python
# 改前
intrinsics = jt.zeros(..., device=pose_encoding.device)

# 改后
intrinsics = jt.zeros(..., dtype="float32")
```

---

## 5. generate_jittor_port.py 其它修正

- 恢复误删的 `layers/rope.py` 专用替换（`position_cache` / `frequency_cache` 类型注解）。
- 新增/修正上述 `heads/utils.py`、`heads/dpt_head.py`、`heads/head_act.py` 的批量生成规则。

---

## 验证命令（WSL）

```bash
cd /mnt/f/y26/vggt-main
source .venv/bin/activate
export cc_path=/usr/bin/g++-12
python demo_jvggt.py \
  --weights model.pt \
  --image_folder examples/kitchen/images/ \
  --inference_only
```

**进度说明（修复前向路径后）**

| 阶段 | 状态 |
|------|------|
| 权重加载 | 通过（~53–60s） |
| Aggregator + CameraHead | 通过 |
| depth_head（DPT pos embed） | 修复 #1 后通过 |
| depth_head（ReLU） | 修复 #2 后通过 |
| point_head（sign/expm1） | 修复 #3 后通过 |
| 前向 `model(images)` | 通过 |
| `predictions_to_numpy` | 修复 #4 后应可继续 |

Parameter 相关 WARNING（`Parameter interface isn't needed in Jittor`）可忽略。

---

## 涉及文件一览

```
jvggt/heads/utils.py       # meshgrid xy 对齐
jvggt/ops.py               # ReLU, sign, expm1
jvggt/heads/dpt_head.py    # 使用 ReLU
jvggt/heads/head_act.py    # 使用 sign, expm1
jvggt/utils/pose_enc.py    # 去掉 .device
tools/generate_jittor_port.py
```
