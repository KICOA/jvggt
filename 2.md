# Viser 可视化与 8GB 显存优化记录（本轮）

本轮主要解决：**viser 点云空白**、**depth 反投影 NaN**、**8GB GPU OOM**、**跳过 depth 后 KeyError** 等问题。修改涉及 `demo_jvggt.py`、`demo_viser.py`、`jvggt/viser_wrapper.py`、`jvggt/utils/geometry_np.py`、`jvggt/utils/pose_enc.py`、`jvggt/inference.py`、`jvggt/models/vggt.py`、`jvggt/heads/dpt_head.py`。未动 `vggt/`、`training/`。

---

## 问题背景

| 现象 | 根因 |
|------|------|
| viser 能连上但看不到点云 | 默认走 depth 反投影；内参 `fy=0` → 除零 → 点云 NaN |
| `geometry_np.py:33 divide by zero` | `pose_enc` 解码 FoV 退化，`fy = (H/2)/tan(fov_h/2) → 0` |
| `Unable to alloc cuda device memory` | 8GB 显存：模型 ~5GB + depth_head DPT ~1.1GB 峰值 |
| `KeyError: 'depth'` | 为省显存跳过 depth head，但 `viser_wrapper` 仍读 `pred_dict["depth"]` |
| `AttributeError: jittor has no attribute 'pi'` | FoV clamp 误用 `jt.pi`（Jittor 无此常量） |

---

## 1. 默认改用 `world_points` 可视化

**原因**

viser 默认可直接用模型输出的 `world_points`，无需 depth + 相机反投影。反投影路径依赖 `intrinsic`，在 Jittor 推理下易出现 `fy=0`。

**修改**

| 文件 | 改动 |
|------|------|
| `demo_jvggt.py` | 默认 `use_point_map=True`；仅 `--use_depth_unproject` 时走 depth 反投影 |
| `demo_jvggt.py` | 新增 `--use_depth_unproject`（替代旧 `--use_point_map` 作为默认行为） |
| `demo_viser.py` | 同步：`--use_depth_unproject` / 默认 world_points |
| `jvggt/viser_wrapper.py` | 默认 `use_point_map=True` |

**终端预期输出**

```
Viser: using predicted world point map
```

不应再出现（除非显式加 `--use_depth_unproject`）：

```
Viser: unprojecting depth maps with predicted cameras
```

---

## 2. `jvggt/viser_wrapper.py` 可视化增强

**改动摘要**

1. **数据预处理**：对 `images` / `world_points` / `conf` 等做 batch 维 squeeze；颜色 clip 到 `[0,1]`
2. **NaN 安全**：`finite_mask` 过滤非有限点；`_nanmean_axis0` 做场景中心化（避免 `np.mean` 遇 NaN 污染）
3. **置信度过滤兜底**：过滤后 0 点时回退显示全部有限点
4. **点大小**：`_auto_point_size()` 按点云范围自适应（约 `extent * 0.003`，clamp 到 `[0.008, 0.08]`）
5. **相机**：`@server.on_client_connect` 自动对准点云；GUI 按钮 **Reset Camera to Point Cloud**
6. **depth 反投影回退**：有效点 <90% 时自动改用 `world_points`
7. **缺少 depth 字段**：depth head 被跳过时，若仍请求 depth 反投影 → 警告并回退 `world_points`（修复 `KeyError: 'depth'`）
8. **诊断日志**：

```
Point cloud: N/M finite points, range [min, max]
Viser: showing K points (size=0.0xxx)
Tip: if the view is empty, click 'Reset Camera to Point Cloud' in the left panel.
```

---

## 3. `jvggt/utils/geometry_np.py` depth 反投影数值稳定

**原因**

即使走 depth 路径，也需防止 `fv=0`、depth 爆炸、NaN 传播。

**修改**

- `_safe_focal_lengths()`：`fu/fv` 非有限或 `|f| < 1e-3` 时设为 `1e-3`
- depth clip 到 `[0, 1e4]`，`nan_to_num`
- 世界坐标用 `@ R.T + t` 替代有问题的 `np.dot` 路径
- 全程 NumPy，无 torch 依赖

---

## 4. `jvggt/utils/pose_enc.py` FoV 与内参修复

**原因**

`fov_h ≈ π` 时 `tan(fov_h/2) → ∞`，`fy → 0`，反投影除零。

**修改**

```python
import math

fov_h = jt.clamp(fov_h, 1e-4, math.pi - 1e-4)
fov_w = jt.clamp(fov_w, 1e-4, math.pi - 1e-4)
```

> **注意**：Jittor **没有** `jt.pi`，必须用 `math.pi`（曾报 `AttributeError: module 'jittor' has no attribute 'pi'`）。

---

## 5. `jvggt/inference.py` 内参 sanitize

**新增** `_sanitize_intrinsics_numpy()`：在转 numpy 后，若 `fx/fy` 非有限或为 0，替换为 `W*0.9` / `H*0.9`；主点默认图像中心。

**扩展** `run_vggt_forward()`：

```python
run_vggt_forward(model, images, frames_chunk_size=1, run_depth=False, run_point=True)
```

- `run_depth=False`：跳过 depth head（省 ~1GB+ 峰值显存）
- `run_point=True`：仍跑 world_points head（viser 必需）

---

## 6. `jvggt/models/vggt.py` 显存优化

**改动**

1. `execute(..., run_depth=True, run_point=True)`：按 flag 选择性跑 head
2. 不再把 `pose_enc_list` 放进 `predictions`（仅保留 `pose_enc` numpy 副本），减少引用占用
3. `aggregator` 之后、`camera_head` / DPT 各阶段后调用 `jt.gc()`
4. depth / world_points 输出仍 `jt.array(x.numpy())` 物化到 CPU 侧副本

---

## 7. `jvggt/heads/dpt_head.py` 分帧物化

**原因**

DPT 多帧一起算时，`B*S` 维特征图在反卷积处峰值显存高（如 `float32[2,512,25,37] → [2,512,50,74]` 需 ~1.1GB）。

**修改**

分 chunk 推理后，每 chunk 立即 `.numpy()` 存 CPU，再 `jt.gc()`，最后 `np.concatenate` 拼回：

```python
all_preds.append(chunk_preds.numpy())
jt.gc()
return jt.array(np.concatenate(all_preds, axis=1)), ...
```

---

## 8. `demo_jvggt.py` CLI 与诊断

**新增 / 调整参数**

| 参数 | 默认 | 说明 |
|------|------|------|
| `--use_depth_unproject` | 关 | 用 depth+相机反投影（不推荐 jvggt 默认路径） |
| `--with_depth` | 关 | 强制跑 depth head（约 +1GB VRAM） |
| `--frames_chunk_size` | **1** | DPT 每次处理帧数，1=最省显存 |
| `--low_vram` | 关 | `max_images=1`、`frames_chunk_size=1` |
| `--use_point_map` | — | 兼容旧参数，等价于默认行为 |

**推理逻辑**

```python
run_depth = args.use_depth_unproject or args.with_depth
# 默认 run_depth=False → 跳过 depth head
```

**`print_prediction_summary()` 新增**

```
camera fx/fy: fx=[...], fy=[...]
pose_enc fov (h,w): [...]
world_points finite: 99.x% of pixels
```

---

## 9. 推荐运行命令

### 8GB 显存（推荐）

```bash
cd /mnt/f/y26/vggt-main
source .venv/bin/activate
export cc_path=/usr/bin/g++-12

python demo_jvggt.py --weights model.pt --image_folder examples/kitchen/images/ --low_vram
```

### 一般 GPU（2 张图）

```bash
python demo_jvggt.py --weights model.pt --image_folder examples/kitchen/images/
```

### 仅推理、不看 viser

```bash
python demo_jvggt.py --inference_only --low_vram
```

### 需要 depth 输出时

```bash
python demo_jvggt.py --with_depth --low_vram   # 仍可能 OOM，优先 --max_images 1
```

### viser 使用注意

- **不要**加 `--inference_only`（会跳过 viser）
- 浏览器打开 `http://127.0.0.1:8080`，**保持终端运行**
- 空白时：点 **Reset Camera to Point Cloud**，Confidence 滑到 **0**
- `(viser) Connection opened ...` 为正常 WebSocket 日志，非报错

---

## 10. 故障排查速查

| 报错 / 现象 | 处理 |
|-------------|------|
| `divide by zero` in geometry_np | 不要用 depth 反投影；确认日志为 `using predicted world point map` |
| `KeyError: 'depth'` | 已修复：viser 在无 depth 时自动用 world_points |
| `jt.pi` AttributeError | 已修复：改用 `math.pi` |
| CUDA OOM on depth_head | 默认已跳过 depth；加 `--low_vram --max_images 1` |
| `finite points: 0` | 模型输出异常，需查 jvggt 推理链路 |
| viser 连接正常但空白 | Reset Camera；检查 `Viser: showing N points` 中 N>0 |

---

## 11. 修改文件清单

```
demo_jvggt.py              CLI、skip depth、low_vram、诊断输出
demo_viser.py              默认 world_points、--use_depth_unproject
jvggt/viser_wrapper.py     点云/相机/回退/缺 depth 兼容
jvggt/utils/geometry_np.py 反投影数值稳定
jvggt/utils/pose_enc.py    FoV clamp (math.pi)
jvggt/inference.py         sanitize intrinsics、run_depth/run_point
jvggt/models/vggt.py       选择性 head、gc
jvggt/heads/dpt_head.py      分 chunk numpy 物化
```

---

## 12. 与 `1.md` 的关系

- **`1.md`**：Jittor 算子/API 移植差异（DPT pos_embed、ReLU、sign/expm1、pose_enc device 等）
- **`2.md`（本文）**：viser 可视化路径、显存策略、pose/intrinsic 数值问题及 demo 用法

两者叠加后，`demo_jvggt.py --low_vram` 在 8GB GPU 上应能：推理 → 输出 `world_points` → viser 显示点云。
