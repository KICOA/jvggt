# VGGT Jittor 推理改造说明（修改.md）

本文档说明如何将 VGGT **推理路径**从 PyTorch 迁移到 [Jittor](https://github.com/Jittor/jittor)，使用官方 `model.pt` 权重，**不修改**原有 `vggt/` 训练与 PyTorch 实现。

---

## 1. 改造目标

| 项目 | 说明 |
|------|------|
| 新包目录 | `jvggt/`（Jittor 推理专用） |
| 权重 | 仍使用 HuggingFace / 本地 **`model.pt`**，经 `jvggt/pt_loader.py` 读取（**无需 torch**） |
| 不改动的部分 | `vggt/`、`training/`、训练配置 |
| 未移植 | `track_head`、`vggt/dependency/` 跟踪链路（推理 demo 不依赖） |

---

## 2. 目录结构

```
jvggt/
├── __init__.py
├── ops.py                 # PyTorch functional 兼容层（ReLU、sign、expm1、attention 等）
├── pt_loader.py           # 无 torch 读取 .pt（zip + pickle + mmap）
├── weight_loader.py       # 权重路径解析与加载
├── inference.py           # create_vggt_model / run_vggt_forward 等高层 API
├── jittor_env.py          # Windows 下自动配置 cl.exe
├── viser_wrapper.py       # 无 torch 的 viser 可视化
├── layers/                # ViT、Attention、RoPE（由 vggt/ 自动生成）
├── models/                # Aggregator、VGGT
├── heads/                 # CameraHead、DPTHead
└── utils/                 # load_fn、pose_enc、rotation、geometry_np

tools/
├── generate_jittor_port.py   # 从 vggt/ 批量生成 jvggt/ 源码
├── convert_pt_to_npz.py      # 可选：model.pt → model.npz 加速二次加载
├── check_forward.py          # 前向 sanity check
└── check_weights.py          # 权重 key 对齐检查

demo_jvggt.py                 # Jittor 推理 + viser 入口
run_demo_jvggt.ps1            # Windows：加载 VS 编译环境后运行 demo
```

---

## 3. 推理数据流

```
图像路径
  ↓
jvggt.utils.load_fn.load_and_preprocess_images  →  jt.Var [N,3,H,W]
  ↓
jvggt.models.VGGT.execute
  ├── Aggregator（DINO + 交替 frame/global attention）
  ├── CameraHead  → pose_enc
  ├── DPTHead (depth，可选)  → depth, depth_conf
  └── DPTHead (point)  → world_points, world_points_conf
  ↓
jvggt.utils.pose_enc.pose_encoding_to_extri_intri  → extrinsic, intrinsic
  ↓
.numpy()  → viser / 可视化
```

**8GB 显存默认策略**（`demo_jvggt.py`）：

- `run_depth=False`：跳过 depth head（约省 1GB+ 峰值显存）
- `frames_chunk_size=1`：DPT 分帧推理，每帧结果立即 `.numpy()` 落 CPU
- 默认可视化走 **`world_points`**，不走 depth 反投影（避免内参退化导致 NaN）

---

## 4. PyTorch → Jittor 主要代码改动

### 4.1 全局替换（`tools/generate_jittor_port.py`）

| PyTorch | Jittor |
|---------|--------|
| `import torch` | `import jittor as jt` |
| `import torch.nn as nn` | `from jittor import nn` |
| `torch.nn.functional as F` | `from jvggt.ops import F` |
| `torch.*` | `jt.*` |
| `from vggt.xxx` | `from jvggt.xxx` |
| `PyTorchModelHubMixin` | 删除 |
| `torch.utils.checkpoint` | 删除（推理恒为全量前向） |

### 4.2 手写兼容层（`jvggt/ops.py`）

| API | 说明 |
|-----|------|
| `F.scaled_dot_product_attention` | 标准 attention 实现 |
| `ReLU` | 替代 `nn.ReLU(inplace=True)`（Jittor 不支持 inplace） |
| `sign` / `expm1` | 替代 Var 上缺失的 `.sign()` / `.expm1()` |
| `trunc_normal_` / `normal_` / `zeros_` | 权重初始化 |
| `nan_to_num` | numpy 风格数值清理 |

### 4.3 按文件特殊处理

| 文件 | 改动 |
|------|------|
| `models/vggt.py` | `execute(..., frames_chunk_size, run_depth, run_point)`；输出物化到 CPU；`jt.gc()` |
| `heads/dpt_head.py` | 分 chunk 推理后 `.numpy()` + `np.concatenate`；`ReLU` 替换 |
| `heads/utils.py` | `meshgrid` 对齐 PyTorch `indexing="xy"`（transpose） |
| `heads/head_act.py` | 使用 `sign` / `expm1` 兼容函数 |
| `utils/pose_enc.py` | FoV clamp（`math.pi`）；去掉 `.device` |
| `utils/load_fn.py` | 去掉 torchvision；PIL → `jt.array`；新增 `list_images_from_folder` |
| `utils/geometry_np.py` | 纯 NumPy depth 反投影（viser 用） |
| `viser_wrapper.py` | 无 torch 依赖；缺 depth 时回退 world_points |

### 4.4 权重加载（`jvggt/weight_loader.py` + `pt_loader.py`）

优先级：

1. 命令行 `--weights`
2. 仓库根目录 `model.npz` → `model.pt`
3. HuggingFace 下载 `model.pt`

```bash
# 可选：一次性转换，加快下次加载
python tools/convert_pt_to_npz.py --input model.pt --output model.npz
```

---

## 5. 使用方式

### 5.1 安装

```bash
pip install jittor
pip install -r requirements_demo.txt   # viser、opencv 等
```

推理读 `model.pt` **不需要安装 torch**。

### 5.2 运行 demo

将 `model.pt` 放在项目根目录，或指定 `--weights`：

```bash
# 默认 examples/kitchen/images/，8 张图（可用 --max_images 调整）
python demo_jvggt.py

# 8GB 显存推荐
python demo_jvggt.py --low_vram

# 仅推理、打印输出 shape
python demo_jvggt.py --inference_only --low_vram

# 与 PyTorch demo_viser 完全一致（全 head + depth 反投影 vis）
python demo_jvggt.py --vggt_compat --max_images 0

# CPU
python demo_jvggt.py --cpu --inference_only
```

### 5.3 Windows 编译问题

Jittor 首次 import 需编译 C++ 扩展。若报 `mspdbcore.dll` / `cl.exe` 错误：

1. 安装 VS 2022 生成工具（「使用 C++ 的桌面开发」）
2. 运行：`.\run_demo_jvggt.ps1 --inference_only`

`demo_jvggt.py` 会在 import Jittor 前调用 `jvggt/jittor_env.py` 设置 `cc_path`。

### 5.4 Python API 示例

```python
import jittor as jt
from jvggt.inference import create_vggt_model, run_vggt_forward, predictions_to_numpy_like_vggt_demo
from jvggt.utils.load_fn import list_images_from_folder, load_and_preprocess_images

jt.flags.use_cuda = 1
model = create_vggt_model()  # 默认 ./model.pt
paths = list_images_from_folder("examples/kitchen/images/")[:2]
images = load_and_preprocess_images(paths)

raw = run_vggt_forward(model, images, frames_chunk_size=1, run_depth=False, run_point=True)
out = predictions_to_numpy_like_vggt_demo(raw, image_hw=images.shape[-2:])
# out: pose_enc, extrinsic, intrinsic, world_points, world_points_conf, ...
```

---

## 6. 重新生成源码

当 `vggt/` 中推理相关文件更新后：

```bash
python tools/generate_jittor_port.py
```

**会覆盖**：`jvggt/layers`、`models`、`heads`、`utils` 下由脚本生成的文件。  
**不会覆盖**：`ops.py`、`inference.py`、`weight_loader.py`、`pt_loader.py`、`viser_wrapper.py` 等手写文件。

重新生成后请确认：

- `heads/head_act.py` 仍使用 `from jvggt.ops import F, expm1, sign`
- `heads/utils.py` 中 meshgrid 已 transpose 对齐 xy
- `models/vggt.py` 中 `execute` 的 `run_depth` / `run_point` 逻辑仍在（该文件为手写维护）

---

## 7. 与 PyTorch 推理的差异

| 项目 | 说明 |
|------|------|
| 混合精度 | PyTorch demo 用 bfloat16/float16；Jittor 版默认 float32 |
| Track 头 | 未移植 |
| 数值一致性 | 未做逐层对齐；DINO bicubic antialias 在 ops 中未实现 |
| 可视化 | `demo_jvggt.py` 默认 world_points；PyTorch `demo_viser.py` 仍用 torch |

---

## 8. 变更摘要

| 类别 | 说明 |
|------|------|
| 新增目录 | `jvggt/`（约 30 个 Python 文件） |
| 新增入口 | `demo_jvggt.py` |
| 新增工具 | `generate_jittor_port.py`、`convert_pt_to_npz.py`、`check_*.py` |
| 修改原仓库 | **`vggt/`、`training/` 未动** |
| 训练影响 | **无** |

---

## 10. 代码审查修复（2025-06）

审查发现并修复以下 Jittor 兼容问题（详见对话记录）：

| 严重级别 | 文件 | 问题 | 修复 |
|---------|------|------|------|
| **阻断** | `layers/block.py` | 残留 `from torch import nn, Tensor` | 改为 `from jittor import nn`，`Tensor` → `jt.Var` |
| **阻断** | `layers/layer_scale.py` 等 | 类型注解 `Tensor` 未定义 | 统一为 `jt.Var` |
| **阻断** | `models/aggregator.py` | `register_buffer` / `FloatTensor` / `.device` | `setattr` + `jt.array`；去掉 device |
| **阻断** | `layers/vision_transformer.py` | `nn.init.normal_` / `.to(dtype)` | `normal_` / `.astype()` |
| **阻断** | `layers/rope.py` | `cartesian_prod` / `device=` / `tokens.device` | `meshgrid` + stack；去掉 device |
| **阻断** | `heads/utils.py` | `pos.device` / `meshgrid indexing="xy"` | 去掉 device；transpose 对齐 |
| **阻断** | `utils/rotation.py` | `.device` / `.max(flr)` / `.size()` | `jt.maximum` / `.shape` |
| **阻断** | `layers/attention.py` | `qkv.unbind(0)` | `jt.unbind(qkv, 0)` |
| 中等 | `heads/head_act.py` | `.clamp(min=)` | `jt.maximum(d, 1e-8)` |
| 中等 | `ops.py` | `jt.expm1` 可能不存在 | `x.exp() - 1.0` |
| 低 | `layers/block.py` 嵌套路径 | `fmha` / `scaled_index_add` 仅训练用 | 推理不走该分支，暂未移植 |

`tools/generate_jittor_port.py` 已同步上述规则，避免重新生成时回归。

---

## 11. 故障排查

| 现象 | 处理 |
|------|------|
| CUDA OOM | `--low_vram --max_images 1`，默认已跳过 depth |
| viser 空白 | 确认日志为 `using predicted world point map`；点 Reset Camera |
| `divide by zero` | 不要用 `--use_depth_unproject`；或检查内参 sanitize |
| `ModuleNotFoundError: torch` | 使用 `jvggt.viser_wrapper`，勿从旧版 `demo_viser` 导入推理 |
| world_points 全零 | 运行 `python tools/check_forward.py` 检查权重加载 |

更多修复细节见仓库内 `1.md`（算子/API 差异）与 `2.md`（viser / 显存优化）。
